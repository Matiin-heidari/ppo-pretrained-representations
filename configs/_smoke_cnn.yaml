run_name: _smoke_cnn

env_id: MiniWorld-OneRoom-v0
seed: 0
n_envs: 4
use_subproc: false

encoder: cnn
encoder_kwargs:
  features_dim: 512

total_timesteps: 4096
checkpoint_steps: [2048, 4096]

learning_rate: 0.0003
n_steps: 256
batch_size: 256
n_epochs: 10
gamma: 0.99

# off so the pilot measures pure training throughput
dormant_ratio:
  enabled: false
