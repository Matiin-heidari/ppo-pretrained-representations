run_name: _smoke_cnn_fourrooms

env_id: MiniWorld-FourRooms-v0
seed: 0
n_envs: 4
use_subproc: false

encoder: cnn
encoder_kwargs:
  features_dim: 512

total_timesteps: 20480
checkpoint_steps: [20480]

# off so the pilot measures pure training throughput / learning signal
dormant_ratio:
  enabled: false
generalization:
  enabled: true

learning_rate: 0.0003
n_steps: 256
batch_size: 256
n_epochs: 10
gamma: 0.99
